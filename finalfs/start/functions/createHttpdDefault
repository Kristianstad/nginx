createHttpdDefault(){
	local file="$VAR_CONFIG_DIR/http.d/default.conf"
	local server_begin=""
	local server_end=";"
	echo -e "server {" > "$file"
	configFromVarGroup server >> "$file"
	echo -e "}\n" >> "$file"
}
